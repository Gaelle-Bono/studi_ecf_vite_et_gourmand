{% extends 'base.html.twig' %}


{% block title %}Modifier une commande{% endblock %}

{% block body %}
    <h1 class="h3 mb-3">Modifier une commande</h1>
    {{ form_start(form, { attr: { 
        novalidate: 'novalidate',
        'data-controller': 'order-form',
        'data-times-url' : path('app_order_available_times'),
        'data-edit': 'true',
        'data-order-id': order.id,
        'data-validate-number-of-people-url': path('app_order_validate_number_of_people'),
        'data-summary-url': path('app_order_summary'),
        'data-cancel-url': path('app_order_show', { id: order.id }),
        'data-action': 'submit->order-form#onSubmit keydown->order-form#handleKeydown',
        'data-turbo': 'false' 
    } })  }}    

    {% if form.vars.errors|length > 0 %}
        <div class="alert alert-danger" role="alert">
            <i class="bi bi-exclamation-triangle me-2"></i>
            <div>
                <strong>Impossible de traiter votre demande</strong><br>
                {{ form_errors(form) }}
            </div>
        </div>
    {% endif %}


    {# stepper #}
    {% include 'pages/order/_steps/_stepper.html.twig' %}

    {# step 1: customer info and service details #}
    {% include 'pages/order/_steps/_step1_customer_service.html.twig' with {
         edit: true,
         menu: order.menu
    } %}

    {# step 2: menu page #}
    {% include 'pages/order/_steps/_step2_menu.html.twig' with {
        edit: true,
        menu: order.menu
    } %}

    {# step 3: number of people #}
    {% include 'pages/order/_steps/_step3_people.html.twig' %}

    {# step 4: order summary and confirmation #}
    {% include 'pages/order/_steps/_step4_summary.html.twig' with {
        edit: true
    } %}
    
    {{ form_row(form._token) }}
        
    {{ form_end(form, {'render_rest': false}) }}

{% endblock %}

